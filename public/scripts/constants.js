// Storage keys and default values for Reader Line extension

const STORAGE_KEYS = {
  COLOR: 'color',
  HEIGHT: 'height',
  ENABLED: 'enabled',
  MODE: 'mode',
};

const DEFAULTS = {
  COLOR: 'rgba(0, 0, 0, 0.15)',
  HEIGHT: 20,
  ENABLED: false,
  MODE: 'line',
};

const HEIGHT_STEP = 2;
